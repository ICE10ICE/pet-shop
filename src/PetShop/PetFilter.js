

function PetFilter({ speciesOptions, breedOptions, colorOptions, sizeOptions, sexOptions, ageRangeOptions, youngestChildAgeOptions, locationOptions, onFilterChange }) {
  // Your PetFilter component code
}

export default PetFilter; // Make sure to export the PetFilter component
